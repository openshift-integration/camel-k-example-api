config:
  namespace:
    temporary: true
pre:
- name: minio installation
  run: |
    oc apply -f minio.yaml

- name: integration
  run: |
    kamel install -w -n ${YAKS_NAMESPACE}

    kamel run --name api MinioCustomizer.java ../API.java --property-file minio.properties --open-api ../openapi.yaml -d camel-openapi-java -n ${YAKS_NAMESPACE}
